// Payout and earnings constants
// NOTE: These are fallback defaults. Use getPlatformCommissionRate() and getMinimumPayoutAmount() 
// from platform-settings-actions.ts for actual values from database.
export const PLATFORM_COMMISSION_RATE = 0.05; // 5% commission (fallback)
export const MINIMUM_PAYOUT_AMOUNT = 1000; // Minimum ₦1000 to request payout (fallback)

